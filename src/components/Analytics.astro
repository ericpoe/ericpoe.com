---
import { PUBLIC_GA_MEASUREMENT_ID } from 'astro:env/client';

const measurementId = import.meta.env.PROD ? PUBLIC_GA_MEASUREMENT_ID : undefined;
---

{measurementId && <script is:inline src="/scripts/analytics.js" data-measurement-id={measurementId}></script>}
